Liste de Lobbys -> ajouter un champ nom pour LobbyModel

createRoom(){ /screens/Create
    //vérifier si user n'est pas déjà host
    //générer id qui n'existe pas
    //créer lobby avec cet id
    //update user's lobby_id
    //return lobby_id
}

joinRoom(){ /screens/join
    //create new user with name
    //vérifier que user n'est pas deja dans le lobby
    //add user to user_lobby
    //return validate
}