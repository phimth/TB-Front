import React from 'react'
import { Button, Form, Container, Row, Col } from 'react-bootstrap'
import {
  useParams,
  RouteComponentProps,
  useHistory,
  useLocation,
} from 'react-router-dom'

const LoginScreen = () => {
  return <Row className="justify-content-center"></Row>
}

export default LoginScreen
